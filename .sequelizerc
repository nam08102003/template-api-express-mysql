const path = require("path");

module.exports = {
  config: path.resolve("src/v1", "config", "config.json"),
  "models-path": path.resolve("src/v1", "models"),
  "migrations-path": path.resolve("src/v1", "migrations"),
  "seeders-path": path.resolve("src/v1", "seeders"),
};
